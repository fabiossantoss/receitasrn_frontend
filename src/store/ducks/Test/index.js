export const Types = {
  GET_TEST: 'Test/GET_TEST',
};

const INITIAL_STATE = {
  reducerTeste: [],
  estudoRedux: '',
};

export default function test(state = INITIAL_STATE, action) {
  return state;
}
